include 'core', 'db-mongo', 'db-postgres', 'db-mysql', 'cli-deploy', 'rest-deploy'

rootProject.name = 'configurationmanager'
rootProject.children.each {
	it.name = rootProject.name + "-" + it.name
}
